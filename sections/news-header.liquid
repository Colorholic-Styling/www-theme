<header class="page-head section section--cream">
  <div class="section__inner">
    <h1 class="page-head__title">{{ heading | escape }}</h1>
    {% if intro != blank %}<p class="page-head__subtitle">{{ intro | escape }}</p>{% endif %}
  </div>
</header>
