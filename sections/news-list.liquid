{% schema %}
{
  "name": "Latest news",
  "settings": [
    {
      "type": "select",
      "id": "theme",
      "label": "Theme",
      "options": [
        {
          "value": "light",
          "label": "Light"
        },
        {
          "value": "cream",
          "label": "Cream"
        },
        {
          "value": "dark",
          "label": "Dark"
        },
        {
          "value": "accent",
          "label": "Accent"
        }
      ],
      "default": "light"
    },
    {
      "type": "text",
      "id": "anchor",
      "label": "Anchor"
    },
    {
      "type": "select",
      "id": "align",
      "label": "Text alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        }
      ],
      "default": "left"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Title"
    },
    {
      "type": "richtext",
      "id": "bodyHtml",
      "label": "Body"
    },
    {
      "type": "url",
      "id": "indexHref",
      "label": "All-news link"
    },
    {
      "type": "text",
      "id": "indexLabel",
      "label": "All-news link label"
    }
  ],
  "blocks": [
    {
      "type": "article",
      "name": "Article",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Title"
        },
        {
          "type": "url",
          "id": "href",
          "label": "Link"
        },
        {
          "type": "textarea",
          "id": "summary",
          "label": "Summary"
        },
        {
          "type": "image_picker",
          "id": "picture",
          "label": "Picture"
        },
        {
          "type": "text",
          "id": "dateText",
          "label": "Display date"
        },
        {
          "type": "text",
          "id": "dateIso",
          "label": "ISO date"
        }
      ]
    }
  ]
}
{% endschema %}

{%- comment -%}
  Resource sources for the same article shape, first non-empty wins:

    section.settings.news — a CMS `news-list` block, projected by src/blocks.ts
    pages_by_type['news'].pages — articles declared by the JSON route template,
                                  newest publish date first
    latestNews            — temporary compatibility alias during rollout
    section.blocks        — articles written into a JSON template by hand

  `default` treats an empty array as absent, so an unpublished news archive
  falls through to the next source rather than rendering an empty grid.
{%- endcomment -%}
{% assign news = section.settings.news | default: pages_by_type['news'].pages | default: latestNews | default: section.blocks %}
<section class="section section--{{ section.settings.theme | escape }}"{% if section.settings.anchor != blank %} id="{{ section.settings.anchor | escape }}"{% endif %}>
  <div class="section__inner">
    {% render '/snippets/section-head', settings: section.settings %}

    {% if news.size > 0 %}
    <ul class="card-grid card-grid--3">
      {% for row in news %}
      {% assign article = row.settings | default: row %}
      {% if article.title != blank and article.href != blank %}
      <li class="card">
        {% if article.picture != blank %}
        <a class="card__media" href="{{ article.href | escape }}">
          <img src="{{ article.picture | escape }}" alt="{{ article.title | escape }}" loading="lazy">
        </a>
        {% endif %}
        <div class="card__body">
          {% if article.dateIso != blank %}<time class="card__date" datetime="{{ article.dateIso | escape }}">{{ article.dateText | escape }}</time>{% endif %}
          <h3 class="card__title"><a href="{{ article.href | escape }}">{{ article.title | escape }}</a></h3>
          {% if article.summary != blank %}<p class="card__summary">{{ article.summary | escape }}</p>{% endif %}
        </div>
      </li>
      {% endif %}
      {% endfor %}
    </ul>

    {% if section.settings.indexHref != blank %}
    <p class="section__more">
      <a class="link-more" href="{{ section.settings.indexHref | escape }}">{% if section.settings.indexLabel != blank %}{{ section.settings.indexLabel | escape }}{% else %}{{ t.backToNews | escape }}{% endif %}</a>
    </p>
    {% endif %}
    {% else %}
    <p class="empty">{{ t.newsEmpty | escape }}</p>
    {% endif %}
  </div>
</section>
