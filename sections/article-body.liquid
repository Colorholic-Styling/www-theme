{% if article.bodyHtml != blank %}
<div class="section section--light">
  <div class="section__inner section__inner--narrow">
    <div class="prose">{{ article.bodyHtml }}</div>
  </div>
</div>
{% endif %}
